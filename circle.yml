dependencies:
  override:
    - chmod +x gradlew
    - ./gradlew publish

general:
  artifacts:
    - "spoils-common/build/libs"
    - "spoils-core/build/libs"
    - "spoils-api/build/libs"

test:
  override:
    - ./gradlew test