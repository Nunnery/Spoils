subprojects {
    apply plugin: "java"
    apply plugin: "maven-publish"

    repositories {
        mavenCentral()
        maven {
            url "https://hub.spigotmc.org/nexus/content/groups/public/"
        }
    }
}